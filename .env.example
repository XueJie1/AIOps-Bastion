# 复制为 .env 并填入非敏感配置 (真实 .env 已被 gitignore)
# 凭证 (API key/SSH 私钥/Token) 不在此文件, 在 Onboarding 时录入 Vault

# LLM Provider 配置 (部署时择一激活 [决策#19])
LLM_ACTIVE_PROVIDER=deepseek
LLM_MODEL=deepseek-v4-pro
LLM_BASE_URL=https://api.deepseek.com/v1

# 应用
BASTION_HOST=0.0.0.0
BASTION_PORT=8080
LOG_LEVEL=INFO
